# ================================
# FRONTEND ENVIRONMENT VARIABLES
# ================================

# Backend API URL (no trailing slash)
# For local development: http://localhost:8000
# For production: https://your-backend-url.onrender.com
VITE_API_URL=http://localhost:8000

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# ================================
# BACKEND ENVIRONMENT VARIABLES
# ================================
# (These are also needed in your backend deployment)

# OpenAI API Key
OPENAI_API_KEY=sk-your-openai-api-key

# Supabase Service Role Key (Required for backend document ingestion to bypass RLS)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Email Service (Optional - for email features)
RESEND_API_KEY=re-your-resend-api-key

# ================================
# INSTRUCTIONS
# ================================
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env to git (it's in .gitignore)
# 4. For deployment:
#    - Vercel: Add VITE_* variables in Vercel dashboard
#    - Render: Add all backend variables in Render dashboard
